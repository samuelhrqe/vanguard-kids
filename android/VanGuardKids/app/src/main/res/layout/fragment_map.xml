<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/mapContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="16dp"
        app:cardBackgroundColor="#222631"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toTopOf="@+id/tvAzimuthValue"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/imgMapBackground"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Mapa da Estrada"
            android:scaleType="centerCrop"
            android:src="@drawable/road" />

    </com.google.android.material.card.MaterialCardView>

    <ImageView
        android:id="@+id/imgCompass"
        android:layout_width="45dp"
        android:layout_height="60dp"
        android:contentDescription="Seta de Navegação"
        android:src="@drawable/navigation"
        app:layout_constraintBottom_toBottomOf="@+id/mapContainer"
        app:layout_constraintEnd_toEndOf="@+id/mapContainer"
        app:layout_constraintHorizontal_bias="0.517"
        app:layout_constraintStart_toStartOf="@+id/mapContainer"
        app:layout_constraintTop_toTopOf="@+id/mapContainer"
        app:layout_constraintVertical_bias="0.857" />

    <TextView
        android:id="@+id/tvAzimuthValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="Azimute: 0° N"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>